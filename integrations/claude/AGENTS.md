# Athena CodeGraph

Call `athena_build_context` before repository work. Use only the returned persona card,
architecture relations, and evidence. Rescan when `athena_status` reports a stale index.
Athena is read-only except for its local `.athena/index.db`.
