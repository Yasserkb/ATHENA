## Athena repository context

For work requiring repository understanding, call `repository_context` once before broad
exploration and reuse its evidence. Skip Athena for general questions or trivial edits in a file
already provided. Use the returned continuation token only when the initial evidence is
insufficient. Verify exact source before editing when confidence is low.
