# Representative Athena benchmark

The order workflow uses `OrderService`, the `order.timeout-ms` configuration key, and the
`V1__create_orders.sql` database migration. The Python FastAPI user endpoint and TypeScript
`CheckoutService` provide cross-language retrieval cases.
